services:
  - type: web
    name: crypto-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python Crypto_Bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
